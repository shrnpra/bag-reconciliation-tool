{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "npm install && npm run build && cd packages/server && npx prisma migrate deploy"
  },
  "deploy": {
    "startCommand": "node packages/server/dist/server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
